from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Example of a DS project with DVC, MLFlow and CDCI',
    author='DEUS Data',
    license='MIT',
)
